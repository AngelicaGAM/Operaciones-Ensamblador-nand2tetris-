//Chip Inc4

/**
 * 4-bit incrementer:
 * out = in + 1 (arithmetic addition)
 */

CHIP Inc4 {
    IN in[4];
    OUT out[4];

    PARTS:
   // Put you code here:
   Add4(a[0..3]=in[0..3], b[0]=true, b[1..3]=false, out[0..3]=out[0..3]);   
}